{% extends "::layout.html.twig" %}


{% block title 'Accueil' %}



{% block header %}
    <h1>Nouveau tweet</h1>
{% endblock %}

{% block content %}

    {{ form_start(formTweet, {'attr': {'class': 'form-horizontal'}}) }}

    {# Les erreurs générales du formulaire. #}
    {{ form_errors(formTweet) }}

    {# Génération du label + error + widget pour un champ. #}
    {{ form_row(formTweet.message) }}

    {# Génération manuelle et éclatée : #}
    {#<div class="form-group">#}
        {# Génération du label. #}
        {#{{ form_label(formTweet.message, "Message", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}#}

        {# Affichage des erreurs pour ce champ précis. #}
        {#{{ form_errors(formTweet.message) }}#}

        {#<div class="col-sm-10">#}
            {# Génération de l'input. #}
            {#{{ form_widget(formTweet.message, {'attr': {'class': 'form-control'}}) }}#}
        {#</div>#}
    {#</div>#}


    <div class="form-group">

        <div class="col-sm-10">
            <input type="submit">
        </div>
    </div>



    {# Génération automatique des champs pas encore écrits.
     Dans cet exemple, ce serait le champ CSRF (géré automatiquement par Symfony !)
     et tous les champs cachés (type « hidden »). #}
    {{ form_rest(formTweet) }}

    {# Fermeture de la balise <form> du formulaire HTML #}
    {{ form_end(formTweet) }}

{% endblock %}